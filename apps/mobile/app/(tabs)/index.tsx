import { MaterialCommunityIcons } from "@expo/vector-icons";
import { useFocusEffect, useRouter } from "expo-router";
import { useCallback, useState } from "react";
import { ActivityIndicator, Pressable, ScrollView, StyleSheet, Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";
import { usePersonalApp } from "../../src/application/PersonalAppProvider";
import type { Quest } from "../../src/domain/types";
import { theme } from "../../src/theme";
export default function TodayScreen() {
  const router = useRouter(); const { repository, revision } = usePersonalApp(); const [quests, setQuests] = useState<Quest[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState<string | null>(null);
  useFocusEffect(useCallback(() => { let active = true; setLoading(true); repository.listActiveQuests().then((items) => { if (active) setQuests(items); }).catch((caught) => { if (active) setError(caught instanceof Error ? caught.message : "Не удалось загрузить квесты"); }).finally(() => { if (active) setLoading(false); }); return () => { active = false; }; }, [repository, revision]));
  const date = new Intl.DateTimeFormat("ru-RU", { weekday: "long", day: "numeric", month: "long" }).format(new Date()).toUpperCase(); const availableXp = quests.reduce((sum, quest) => sum + quest.xpReward, 0); const maintenanceCount = quests.filter((quest) => quest.xpReward === 0).length;
  return <SafeAreaView style={styles.safe} edges={["top"]}><ScrollView contentContainerStyle={styles.content}>
    <View style={styles.header}><View><Text style={styles.eyebrow}>{date}</Text><Text style={styles.title}>Сегодня</Text></View><Pressable onPress={() => router.push("/setup")} style={styles.addButton} accessibilityLabel="Создать путь"><MaterialCommunityIcons name="plus" size={24} color="#FFFFFF" /></Pressable></View>
    <View style={styles.hero}><View style={styles.heroTop}><View><Text style={styles.heroLabel}>Локальный фокус</Text><Text style={styles.heroTitle}>{quests.length ? `${quests.length} активных действия` : "Начни с одного пути"}</Text></View><MaterialCommunityIcons name="shield-check-outline" size={24} color={theme.colors.blue} /></View><View style={styles.heroMeta}><Text style={styles.metaStrong}>{availableXp} XP доступно</Text><Text style={styles.meta}>{maintenanceCount ? `${maintenanceCount} без награды` : "данные на устройстве"}</Text></View></View>
    <View style={styles.sectionRow}><Text style={styles.sectionTitle}>Следующие действия</Text><Text style={styles.sectionCount}>{quests.length}</Text></View>{loading ? <ActivityIndicator style={styles.loader} color={theme.colors.blue} /> : null}{error ? <Text style={styles.error}>{error}</Text> : null}
    {!loading && quests.length === 0 ? <View style={styles.empty}><View style={styles.emptyIcon}><MaterialCommunityIcons name="map-marker-path" size={28} color={theme.colors.blue} /></View><Text style={styles.emptyTitle}>Создай первый путь</Text><Text style={styles.emptyText}>Одна цель, один навык и один квест. Этого достаточно, чтобы начать видеть настоящий прогресс.</Text><Pressable onPress={() => router.push("/setup")} style={styles.emptyButton}><Text style={styles.emptyButtonText}>Создать путь</Text></Pressable></View> : null}
    {quests.map((quest, index) => <Pressable key={quest.id} onPress={() => router.push({ pathname: "/quest/[id]", params: { id: quest.id } })} style={({ pressed }) => [styles.quest, pressed && styles.pressed]}><View style={[styles.questIndex, index === 0 && styles.questIndexActive]}><Text style={[styles.questIndexText, index === 0 && styles.questIndexTextActive]}>{index + 1}</Text></View><View style={styles.questBody}><Text style={styles.questTitle}>{quest.title}</Text>{quest.description ? <Text style={styles.questDescription}>{quest.description}</Text> : null}<View style={styles.questMeta}><Text style={styles.tag}>L{quest.supportLevel}</Text><Text style={quest.xpReward === 0 ? styles.maintenanceTag : styles.xp}>{quest.xpReward === 0 ? "Поддержание · без XP" : `+${quest.xpReward} XP`}</Text></View></View><MaterialCommunityIcons name="chevron-right" size={24} color={theme.colors.muted} /></Pressable>)}
    <Pressable onPress={() => router.push("/(tabs)/review")} style={({ pressed }) => [styles.reviewCard, pressed && styles.pressed]} accessibilityRole="button" accessibilityLabel="Открыть обзор недели"><View style={styles.reviewIcon}><MaterialCommunityIcons name="calendar-week" size={22} color={theme.colors.green} /></View><View style={styles.reviewBody}><Text style={styles.reviewTitle}>Открыть обзор недели</Text><Text style={styles.reviewText}>Собери факты, зафиксируй вывод и оставь одно решение на следующую неделю</Text></View><MaterialCommunityIcons name="chevron-right" size={24} color={theme.colors.muted} /></Pressable>
  </ScrollView></SafeAreaView>;
}
const styles = StyleSheet.create({ safe: { flex: 1, backgroundColor: theme.colors.canvas }, content: { paddingHorizontal: 20, paddingTop: 16, paddingBottom: 40, gap: 16 }, header: { flexDirection: "row", justifyContent: "space-between", alignItems: "center", marginBottom: 4 }, eyebrow: { color: theme.colors.muted, fontSize: 12, letterSpacing: 1.1, fontWeight: "700" }, title: { color: theme.colors.text, fontSize: 36, lineHeight: 42, fontWeight: "700", marginTop: 4 }, addButton: { width: 46, height: 46, borderRadius: 14, backgroundColor: theme.colors.blue, alignItems: "center", justifyContent: "center" }, hero: { backgroundColor: theme.colors.surface, borderRadius: 18, borderWidth: 1, borderColor: theme.colors.border, padding: 18, gap: 16 }, heroTop: { flexDirection: "row", justifyContent: "space-between" }, heroLabel: { color: theme.colors.muted, fontSize: 13, marginBottom: 5 }, heroTitle: { color: theme.colors.text, fontSize: 20, fontWeight: "700", maxWidth: 260 }, heroMeta: { flexDirection: "row", justifyContent: "space-between" }, metaStrong: { color: theme.colors.text, fontWeight: "700" }, meta: { color: theme.colors.muted }, sectionRow: { flexDirection: "row", alignItems: "center", gap: 8, marginTop: 8 }, sectionTitle: { color: theme.colors.text, fontSize: 20, fontWeight: "700" }, sectionCount: { color: theme.colors.muted, backgroundColor: theme.colors.soft, paddingHorizontal: 8, paddingVertical: 2, borderRadius: 8, fontWeight: "700" }, loader: { marginVertical: 28 }, error: { color: theme.colors.red, backgroundColor: "#FCE9E7", borderRadius: 10, padding: 12 }, empty: { backgroundColor: theme.colors.surface, borderWidth: 1, borderColor: theme.colors.border, borderRadius: 18, padding: 22, alignItems: "flex-start" }, emptyIcon: { width: 48, height: 48, borderRadius: 14, backgroundColor: theme.colors.blueSoft, alignItems: "center", justifyContent: "center", marginBottom: 16 }, emptyTitle: { color: theme.colors.text, fontSize: 20, fontWeight: "700" }, emptyText: { color: theme.colors.muted, fontSize: 15, lineHeight: 22, marginTop: 7 }, emptyButton: { minHeight: 48, borderRadius: 11, backgroundColor: theme.colors.blue, alignItems: "center", justifyContent: "center", alignSelf: "stretch", marginTop: 18 }, emptyButtonText: { color: "#FFFFFF", fontSize: 16, fontWeight: "800" }, quest: { minHeight: 104, backgroundColor: theme.colors.surface, borderWidth: 1, borderColor: theme.colors.border, borderRadius: 16, padding: 16, flexDirection: "row", alignItems: "flex-start", gap: 12 }, pressed: { opacity: 0.72 }, questIndex: { width: 34, height: 34, borderRadius: 10, backgroundColor: theme.colors.soft, justifyContent: "center", alignItems: "center" }, questIndexActive: { backgroundColor: theme.colors.blue }, questIndexText: { color: theme.colors.muted, fontWeight: "800" }, questIndexTextActive: { color: "#FFFFFF" }, questBody: { flex: 1, gap: 5 }, questTitle: { color: theme.colors.text, fontSize: 16, fontWeight: "700" }, questDescription: { color: theme.colors.muted, lineHeight: 20 }, questMeta: { flexDirection: "row", flexWrap: "wrap", gap: 8, marginTop: 4 }, tag: { color: theme.colors.blue, backgroundColor: theme.colors.blueSoft, paddingHorizontal: 8, paddingVertical: 3, borderRadius: 6, fontSize: 12, fontWeight: "700" }, xp: { color: theme.colors.green, backgroundColor: theme.colors.greenSoft, paddingHorizontal: 8, paddingVertical: 3, borderRadius: 6, fontSize: 12, fontWeight: "700" }, maintenanceTag: { color: theme.colors.muted, backgroundColor: theme.colors.soft, paddingHorizontal: 8, paddingVertical: 3, borderRadius: 6, fontSize: 12, fontWeight: "700" }, reviewCard: { marginTop: 8, backgroundColor: theme.colors.greenSoft, borderRadius: 16, padding: 16, flexDirection: "row", alignItems: "center", gap: 12 }, reviewIcon: { width: 42, height: 42, borderRadius: 12, backgroundColor: theme.colors.surface, justifyContent: "center", alignItems: "center" }, reviewBody: { flex: 1 }, reviewTitle: { color: theme.colors.text, fontWeight: "700", fontSize: 16 }, reviewText: { color: theme.colors.muted, marginTop: 3, lineHeight: 19 } });
