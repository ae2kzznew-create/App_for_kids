import type { SQLiteDatabase } from "expo-sqlite";
import type { CompletedQuestSummary, PersonalRepository } from "../domain/repository";
import type { ExternalNoteEntityType, ExternalNoteLink, Goal, Profile, ProgressEvent, Quest, QuestCompletion, QuestSkill, Skill, SkillEdge, WeeklyReview } from "../domain/types";
export class SQLitePersonalRepository implements PersonalRepository {
  constructor(private readonly db: SQLiteDatabase) {}
  async ensureProfile(profile: Profile) { await this.db.runAsync("INSERT INTO profiles (id, display_name, created_at, updated_at) VALUES (?, ?, ?, ?) ON CONFLICT(id) DO UPDATE SET display_name = excluded.display_name, updated_at = excluded.updated_at", profile.id, profile.displayName, profile.createdAt, profile.updatedAt); }
  async getGoal(id: string) { const row = await this.db.getFirstAsync<GoalRow>("SELECT * FROM goals WHERE id = ?", id); return row ? mapGoal(row) : null; } async saveGoal(goal: Goal) { await this.db.runAsync("INSERT INTO goals (id, profile_id, title, description, status, created_at, updated_at) VALUES (?, ?, ?, ?, ?, ?, ?) ON CONFLICT(id) DO UPDATE SET title = excluded.title, description = excluded.description, status = excluded.status, updated_at = excluded.updated_at", goal.id, goal.profileId, goal.title, goal.description ?? null, goal.status, goal.createdAt, goal.updatedAt); } async listGoals(profileId: string) { return (await this.db.getAllAsync<GoalRow>("SELECT * FROM goals WHERE profile_id = ? ORDER BY updated_at DESC", profileId)).map(mapGoal); }
  async getSkill(id: string) { const row = await this.db.getFirstAsync<SkillRow>("SELECT * FROM skills WHERE id = ?", id); return row ? mapSkill(row) : null; }
  async saveSkill(skill: Skill) { await this.db.runAsync("INSERT INTO skills (id, goal_id, title, description, support_level, status, mastery_score, next_review_at, created_at, updated_at) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?) ON CONFLICT(id) DO UPDATE SET title = excluded.title, description = excluded.description, support_level = excluded.support_level, status = excluded.status, mastery_score = excluded.mastery_score, next_review_at = excluded.next_review_at, updated_at = excluded.updated_at", skill.id, skill.goalId, skill.title, skill.description ?? null, skill.supportLevel, skill.status, skill.masteryScore, skill.nextReviewAt ?? null, skill.createdAt, skill.updatedAt); }
  async saveReviewedSkillWithEvent(skill: Skill, event: ProgressEvent) { await this.db.withTransactionAsync(async () => { await this.db.runAsync("UPDATE skills SET support_level = ?, status = ?, mastery_score = ?, next_review_at = ?, updated_at = ? WHERE id = ?", skill.supportLevel, skill.status, skill.masteryScore, skill.nextReviewAt ?? null, skill.updatedAt, skill.id); await this.db.runAsync("INSERT INTO progress_events (id, event_type, entity_id, occurred_at, xp_delta, metadata_json) VALUES (?, ?, ?, ?, ?, ?)", event.id, event.type, event.entityId, event.occurredAt, event.xpDelta, event.metadata ? JSON.stringify(event.metadata) : null); }); }
  async listSkills(goalId: string) { return (await this.db.getAllAsync<SkillRow>("SELECT * FROM skills WHERE goal_id = ? ORDER BY updated_at DESC", goalId)).map(mapSkill); } async listSkillsForProfile(profileId: string) { return (await this.db.getAllAsync<SkillRow>("SELECT s.* FROM skills s JOIN goals g ON g.id = s.goal_id WHERE g.profile_id = ? AND g.status != 'archived' ORDER BY s.updated_at DESC", profileId)).map(mapSkill); }
  async saveSkillEdge(edge: SkillEdge) { await this.db.runAsync("INSERT INTO skill_edges (id, parent_skill_id, child_skill_id) VALUES (?, ?, ?)", edge.id, edge.parentSkillId, edge.childSkillId); } async deleteSkillEdge(id: string) { await this.db.runAsync("DELETE FROM skill_edges WHERE id = ?", id); } async listSkillEdges() { return this.db.getAllAsync<SkillEdge>("SELECT id, parent_skill_id AS parentSkillId, child_skill_id AS childSkillId FROM skill_edges ORDER BY id"); }
  async getQuest(id: string) { const row = await this.db.getFirstAsync<QuestRow>("SELECT * FROM quests WHERE id = ?", id); return row ? mapQuest(row) : null; } async saveQuest(quest: Quest, links: QuestSkill[]) { await this.db.withTransactionAsync(async () => { await this.db.runAsync("INSERT INTO quests (id, title, description, support_level, status, xp_reward, scheduled_for, created_at, updated_at) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?) ON CONFLICT(id) DO UPDATE SET title = excluded.title, description = excluded.description, support_level = excluded.support_level, status = excluded.status, xp_reward = excluded.xp_reward, scheduled_for = excluded.scheduled_for, updated_at = excluded.updated_at", quest.id, quest.title, quest.description ?? null, quest.supportLevel, quest.status, quest.xpReward, quest.scheduledFor ?? null, quest.createdAt, quest.updatedAt); await this.db.runAsync("DELETE FROM quest_skills WHERE quest_id = ?", quest.id); for (const link of links) await this.db.runAsync("INSERT INTO quest_skills (quest_id, skill_id, contribution_weight) VALUES (?, ?, ?)", link.questId, link.skillId, link.contributionWeight); }); }
  async listQuests() { return (await this.db.getAllAsync<QuestRow>("SELECT * FROM quests ORDER BY updated_at DESC")).map(mapQuest); } async listQuestSkills(questId: string) { return this.db.getAllAsync<QuestSkill>("SELECT quest_id AS questId, skill_id AS skillId, contribution_weight AS contributionWeight FROM quest_skills WHERE quest_id = ?", questId); } async listActiveQuests() { return (await this.db.getAllAsync<QuestRow>("SELECT * FROM quests WHERE status = 'active' ORDER BY scheduled_for IS NULL, scheduled_for, updated_at DESC")).map(mapQuest); }
  async saveCompletionWithEvent(completion: QuestCompletion, event: ProgressEvent, completedQuest: Quest) { await this.db.withTransactionAsync(async () => { await this.db.runAsync("INSERT INTO quest_completions (id, quest_id, completed_at, evidence_note, evidence_url, reflection, xp_granted) VALUES (?, ?, ?, ?, ?, ?, ?)", completion.id, completion.questId, completion.completedAt, completion.evidenceNote ?? null, completion.evidenceUrl ?? null, completion.reflection ?? null, completion.xpGranted); await this.db.runAsync("INSERT INTO progress_events (id, event_type, entity_id, occurred_at, xp_delta, metadata_json) VALUES (?, ?, ?, ?, ?, ?)", event.id, event.type, event.entityId, event.occurredAt, event.xpDelta, event.metadata ? JSON.stringify(event.metadata) : null); await this.db.runAsync("UPDATE quests SET status = ?, updated_at = ? WHERE id = ?", completedQuest.status, completedQuest.updatedAt, completedQuest.id); }); }
  async listCompletions(questId: string) { return (await this.db.getAllAsync<CompletionRow>("SELECT * FROM quest_completions WHERE quest_id = ? ORDER BY completed_at DESC", questId)).map(mapCompletion); } async listCompletedQuests(limit = 50) { return this.queryHistory("", [], limit); } async listSkillHistory(skillId: string, limit = 50) { return this.queryHistory("WHERE EXISTS (SELECT 1 FROM quest_skills filter_qs WHERE filter_qs.quest_id = q.id AND filter_qs.skill_id = ?)", [skillId], limit); }
  async getWeeklyReview(id: string) { const row = await this.db.getFirstAsync<WeeklyReviewRow>("SELECT * FROM weekly_reviews WHERE id = ?", id); return row ? mapWeeklyReview(row) : null; }
  async saveImportedWeeklyReview(review: WeeklyReview) { await this.db.runAsync("INSERT INTO weekly_reviews (id, profile_id, week_start, achievements, blockers, decisions, completed_at) VALUES (?, ?, ?, ?, ?, ?, ?) ON CONFLICT(id) DO UPDATE SET profile_id = excluded.profile_id, week_start = excluded.week_start, achievements = excluded.achievements, blockers = excluded.blockers, decisions = excluded.decisions, completed_at = excluded.completed_at", review.id, review.profileId, review.weekStart, review.achievements ?? null, review.blockers ?? null, review.decisions, review.completedAt); }
  async saveWeeklyReviewWithEvent(review: WeeklyReview, event: ProgressEvent) { await this.db.withTransactionAsync(async () => { await this.db.runAsync("INSERT INTO weekly_reviews (id, profile_id, week_start, achievements, blockers, decisions, completed_at) VALUES (?, ?, ?, ?, ?, ?, ?)", review.id, review.profileId, review.weekStart, review.achievements ?? null, review.blockers ?? null, review.decisions, review.completedAt); await this.db.runAsync("INSERT INTO progress_events (id, event_type, entity_id, occurred_at, xp_delta, metadata_json) VALUES (?, ?, ?, ?, ?, ?)", event.id, event.type, event.entityId, event.occurredAt, event.xpDelta, event.metadata ? JSON.stringify(event.metadata) : null); }); } async listWeeklyReviews(profileId: string, limit = 12) { const safeLimit = Math.max(1, Math.min(52, Math.floor(limit))); return (await this.db.getAllAsync<WeeklyReviewRow>("SELECT * FROM weekly_reviews WHERE profile_id = ? ORDER BY completed_at DESC LIMIT ?", profileId, safeLimit)).map(mapWeeklyReview); } async listProgressEvents(entityId: string) { return (await this.db.getAllAsync<EventRow>("SELECT * FROM progress_events WHERE entity_id = ? ORDER BY occurred_at DESC", entityId)).map(mapEvent); }
  async getExternalNoteLink(entityType: ExternalNoteEntityType, entityId: string) { const row = await this.db.getFirstAsync<ExternalNoteRow>("SELECT * FROM external_note_links WHERE entity_type = ? AND entity_id = ? ORDER BY updated_at DESC LIMIT 1", entityType, entityId); return row ? mapExternalNote(row) : null; }
  async saveExternalNoteLink(link: ExternalNoteLink) { await this.db.runAsync("INSERT INTO external_note_links (id, entity_type, entity_id, provider, external_path, external_url, updated_at) VALUES (?, ?, ?, ?, ?, ?, ?) ON CONFLICT(id) DO UPDATE SET entity_type = excluded.entity_type, entity_id = excluded.entity_id, provider = excluded.provider, external_path = excluded.external_path, external_url = excluded.external_url, updated_at = excluded.updated_at", link.id, link.entityType, link.entityId, link.provider, link.externalPath ?? null, link.externalUrl ?? null, link.updatedAt); }
  async deleteExternalNoteLink(entityType: ExternalNoteEntityType, entityId: string) { await this.db.runAsync("DELETE FROM external_note_links WHERE entity_type = ? AND entity_id = ?", entityType, entityId); }
  private async queryHistory(where: string, params: Array<string | number>, limit: number): Promise<CompletedQuestSummary[]> { const safeLimit = Math.max(1, Math.min(200, Math.floor(limit))); const rows = await this.db.getAllAsync<HistoryRow>(`SELECT q.id AS quest_id, q.title, c.completed_at, c.xp_granted, c.evidence_note, c.reflection, GROUP_CONCAT(qs.skill_id) AS skill_ids FROM quest_completions c JOIN quests q ON q.id = c.quest_id LEFT JOIN quest_skills qs ON qs.quest_id = q.id ${where} GROUP BY c.id, q.id ORDER BY c.completed_at DESC LIMIT ?`, ...params, safeLimit); return rows.map((row) => ({ questId: row.quest_id, title: row.title, completedAt: row.completed_at, xpGranted: row.xp_granted, evidenceNote: row.evidence_note ?? undefined, reflection: row.reflection ?? undefined, skillIds: row.skill_ids ? row.skill_ids.split(",") : [] })); }
}
type GoalRow = { id: string; profile_id: string; title: string; description: string | null; status: Goal["status"]; created_at: string; updated_at: string }; type SkillRow = { id: string; goal_id: string; title: string; description: string | null; support_level: Skill["supportLevel"]; status: Skill["status"]; mastery_score: number; next_review_at: string | null; created_at: string; updated_at: string }; type QuestRow = { id: string; title: string; description: string | null; support_level: Quest["supportLevel"]; status: Quest["status"]; xp_reward: number; scheduled_for: string | null; created_at: string; updated_at: string }; type CompletionRow = { id: string; quest_id: string; completed_at: string; evidence_note: string | null; evidence_url: string | null; reflection: string | null; xp_granted: number }; type EventRow = { id: string; event_type: ProgressEvent["type"]; entity_id: string; occurred_at: string; xp_delta: number; metadata_json: string | null }; type HistoryRow = { quest_id: string; title: string; completed_at: string; xp_granted: number; evidence_note: string | null; reflection: string | null; skill_ids: string | null }; type WeeklyReviewRow = { id: string; profile_id: string; week_start: string; achievements: string | null; blockers: string | null; decisions: string; completed_at: string }; type ExternalNoteRow = { id: string; entity_type: ExternalNoteEntityType; entity_id: string; provider: ExternalNoteLink["provider"]; external_path: string | null; external_url: string | null; updated_at: string };
const mapGoal = (row: GoalRow): Goal => ({ id: row.id, profileId: row.profile_id, title: row.title, description: row.description ?? undefined, status: row.status, createdAt: row.created_at, updatedAt: row.updated_at }); const mapSkill = (row: SkillRow): Skill => ({ id: row.id, goalId: row.goal_id, title: row.title, description: row.description ?? undefined, supportLevel: row.support_level, status: row.status, masteryScore: row.mastery_score, nextReviewAt: row.next_review_at ?? undefined, createdAt: row.created_at, updatedAt: row.updated_at }); const mapQuest = (row: QuestRow): Quest => ({ id: row.id, title: row.title, description: row.description ?? undefined, supportLevel: row.support_level, status: row.status, xpReward: row.xp_reward, scheduledFor: row.scheduled_for ?? undefined, createdAt: row.created_at, updatedAt: row.updated_at }); const mapCompletion = (row: CompletionRow): QuestCompletion => ({ id: row.id, questId: row.quest_id, completedAt: row.completed_at, evidenceNote: row.evidence_note ?? undefined, evidenceUrl: row.evidence_url ?? undefined, reflection: row.reflection ?? undefined, xpGranted: row.xp_granted }); const mapEvent = (row: EventRow): ProgressEvent => ({ id: row.id, type: row.event_type, entityId: row.entity_id, occurredAt: row.occurred_at, xpDelta: row.xp_delta, metadata: row.metadata_json ? JSON.parse(row.metadata_json) as ProgressEvent["metadata"] : undefined }); const mapWeeklyReview = (row: WeeklyReviewRow): WeeklyReview => ({ id: row.id, profileId: row.profile_id, weekStart: row.week_start, achievements: row.achievements ?? undefined, blockers: row.blockers ?? undefined, decisions: row.decisions, completedAt: row.completed_at }); const mapExternalNote = (row: ExternalNoteRow): ExternalNoteLink => ({ id: row.id, entityType: row.entity_type, entityId: row.entity_id, provider: row.provider, externalPath: row.external_path ?? undefined, externalUrl: row.external_url ?? undefined, updatedAt: row.updated_at });
